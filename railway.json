{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ALWAYS",
    "ports": [
      {
        "port": "$PORT",
        "protocol": "http"
      }
    ]
  }
}